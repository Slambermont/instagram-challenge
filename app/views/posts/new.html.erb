<h1> Add a new post </h1>

<%= form_for @post, html: { multipart: true } do |f| %>
  <p>
    <%= f.label :image %>
    <%= f.file_field :image %>
  </p>

  <p>
    <%= f.label :message %>
    <%= f.text_area :message %>
  </p>

  <p>
    <%= f.submit 'Add a New Post' %>
  </p>
<% end %>
